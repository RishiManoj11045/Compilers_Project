float multiply(int , int );
float multiply(int x, int y) {
    #Function with mismatched return type
    return x * y; 
}

main() {
    int e=0;
    int d =0;
    for(int i=0; i<5; i++) {
        d = d%i;
        if(d == 1) {
            d = e+5;
            break;
        }
        else {
            d = d + 2;
            Foo(c1,c2,a,b);
            continue;
        }
    }
    input(d);
    output: d<<end<<d+e<<"Hi"<<end;
    float result = multiply(x, y); 
    return 0;
}