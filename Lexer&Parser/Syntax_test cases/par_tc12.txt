int Foo(BTree<int> , BSTree<int>,Node<int>,Node<int>);

int Foo(BTree<int> a, BSTree<int> b,Node<int> n1,Node<int> n2) {
    a.search(n1);
    b.delete(n2);
}

main() {
    int e=0;
    Node<int> a = {1, null, null};
    Node<int> b = {2, a, null};
    a.val = 5;
    a.left = b;
    int d = 1;
    BTree<int> c1 = {1, null, 2,4,4,5,5};
    BSTree<int> c2 = {1, 3, 2};
    Foo(c1,c2,a,b);
    for(int i=0; i<5; i++) {
        d = d%i;
        if(d == 1) {
            d = e+5;
            break;
        }
        else {
            d = d + 2;
            continue;
        }
    }
    input(d);
    output: d<<end<<d+e<<"Main"<<end;
    return true;
}